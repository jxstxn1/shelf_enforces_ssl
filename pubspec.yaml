name: shelf_enforces_ssl
description: Enforces that users can only make API requests over HTTPS (SSL).
version: 1.2.0
homepage: https://jxstxn.de
repository: https://github.com/jxstxn1/shelf_enforces_ssl
issue_tracker: https://github.com/jxstxn1/shelf_enforces_ssl/issues

topics:
  - shelf
  - security
  - dart
  - dart_frog
  - dartlang
  - middleware
  - ssl
  - https

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies: 
  shelf: ^1.3.0

dev_dependencies:
  lint: ^2.0.0
  test: ^1.24.0